version: "3.9"

services:
  semantic-search:
    build: .
    ports:
      - "8000:8000"
    environment:
      - CACHE_THRESHOLD=0.90
    volumes:
      - model-cache:/root/.cache/huggingface
    restart: unless-stopped

volumes:
  model-cache:
